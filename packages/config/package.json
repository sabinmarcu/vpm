{
  "name": "@nvpm/config",
  "version": "1.0.0",
  "description": "Configuration for nvpm",
  "main": "lib/index.js",
  "module": "src/index.ts",
  "author": "Sabin Marcu <sabinmarcu@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "build:watch": "nodemon -e ts,tsx -x yarn build",
    "clean": "run-s clean:*",
    "clean:lib": "rm -rf lib",
    "clean:cache": "rm -rf tsconfig.build.tsbuildinfo",
    "prepublish": "yarn build"
  },
  "devDependencies": {
    "@types/mkdirp": "^1.0.1",
    "@types/react": "^16.9.43"
  },
  "dependencies": {
    "hasbin": "^1.2.3",
    "is-npm": "^5.0.0",
    "mkdirp": "^1.0.4",
    "react": "^16.13.1"
  }
}
